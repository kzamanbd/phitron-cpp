#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s, f;
    getline(cin, s);
    // cin.ignore();
    cin >> f;
    int count = 0;
    // while (s.find(f))
    // {
    //     count++;
    // }
    cout << f;

    return 0;
}